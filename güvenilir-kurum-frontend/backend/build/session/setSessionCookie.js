"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.setSessionCookie = void 0;
const jsonwebtoken_1 = __importDefault(require("jsonwebtoken"));
const config_1 = require("../config");
/**
 * Saves the session values as a JSON-Web-Token on a Cookie of the browser, called 'sessionJWT'.
 */
function setSessionCookie(payload, response) {
    // Create a Json-Web-Token:
    // set the expiration of JWT same as the Cookie
    const jwtOptions = {
        expiresIn: `${config_1.cookieOptions.maxAge} seconds`
    };
    const token = jsonwebtoken_1.default.sign(payload, config_1.JWT_SIGNER_SECRET, jwtOptions);
    // Set a Cookie in the header including the JWT and our options:
    response.cookie('sessionJWT', token, config_1.cookieOptions);
    console.log("The JSON-Web-Token with Session Values generated by the backend was saved on the 'sessionJWT'-Cookie. \n");
}
exports.setSessionCookie = setSessionCookie;
